# Tarayıcı tarafı savunmaları.
#
# Ayrı dosyada tutulmalarının nedeni nginx'in `add_header` kalıtım kuralı:
# bir iç blokta tek bir add_header tanımlamak, dış bloktan gelen TÜM
# add_header satırlarını sessizce iptal eder. Bu yüzden başlık ekleyen her
# bloğun bu dosyayı `include` etmesi gerekiyor.

add_header X-Content-Type-Options    "nosniff"          always;
add_header X-Frame-Options           "DENY"             always;
add_header Referrer-Policy           "no-referrer"      always;
add_header Permissions-Policy        "geolocation=(), microphone=(), camera=()" always;
# connect-src'a wss: gerekiyor: terminal ve metrik akışı WebSocket üzerinden.
add_header Content-Security-Policy   "default-src 'self'; script-src 'self'; style-src 'self' 'unsafe-inline'; img-src 'self' data:; font-src 'self' data:; connect-src 'self' ws: wss:; frame-ancestors 'none'; base-uri 'self'; form-action 'self'" always;
